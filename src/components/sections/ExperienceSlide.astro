---
import { Image } from 'astro:assets'

interface Props {
  company: string
  role: string
  date: string
  bullets: readonly string[]
  techStack: readonly string[]
  logoSrc: ImageMetadata
}

const { company, role, date, bullets, techStack, logoSrc } = Astro.props
---

<div
  data-experience-slide
  class="flex h-full w-[85vw] flex-shrink-0 items-center px-6 md:w-[60vw] lg:w-[50vw]"
>
  <div class="mx-auto w-full max-w-lg rounded-2xl bg-bg-elevated p-8 ring-1 ring-white/5">
    <div class="flex items-center gap-4" data-slide-content>
      <div class="h-12 w-12 overflow-hidden rounded-lg bg-white/5 p-2 flex items-center justify-center">
        <Image
          src={logoSrc}
          alt={`${company} logo`}
          class="h-full w-full object-contain"
          width={48}
          height={48}
        />
      </div>
      <div>
        <h3 class="font-display text-xl font-bold">{company}</h3>
        <p class="text-sm text-text-secondary">{role}</p>
      </div>
    </div>

    <p class="mt-2 text-xs text-text-muted" data-slide-content>{date}</p>

    <ul class="mt-5 space-y-3">
      {bullets.map((bullet) => (
        <li class="flex gap-3 text-sm leading-relaxed text-text-secondary" data-slide-content>
          <span class="mt-1.5 h-1.5 w-1.5 flex-shrink-0 rounded-full bg-accent-violet" />
          <span>{bullet}</span>
        </li>
      ))}
    </ul>

    <div class="mt-6 flex flex-wrap gap-2">
      {techStack.map((tech) => (
        <span
          data-tech-pill
          class="rounded-full bg-accent-violet/10 px-3 py-1 text-xs font-medium text-accent-purple ring-1 ring-accent-violet/20"
        >
          {tech}
        </span>
      ))}
    </div>
  </div>
</div>
